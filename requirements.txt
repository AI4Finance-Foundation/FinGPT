# data handling
numpy<2  # NumPy 2.x compatibility issues with PyTorch and other ML dependencies
pandas

# packaging
setuptools>=41.4.0
wheel>=0.33.6

# historical data
tushare

# Core ML dependencies
transformers>=4.32.0
peft>=0.5.0
sentencepiece
accelerate
torch
datasets
# ML dependencies (for GPU acceleration compatibility)
bitsandbytes>=0.43.1
# Triton is installed separately for supported NVIDIA GPU environments.
